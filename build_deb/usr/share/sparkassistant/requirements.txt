# Core automation library
playwright>=1.40.0
fastapi>=0.100.0
uvicorn>=0.22.0

# Note: GTK4 Python bindings (PyGObject) are typically installed via system package manager:
# Ubuntu/Debian: sudo apt install python3-gi python3-gi-cairo gir1.2-gtk-4.0 gir1.2-adw-1
# Fedora: sudo dnf install python3-gobject gtk4 libadwaita
# 
# When creating your virtual environment, ensure you use the --system-site-packages flag:
# python -m venv .venv --system-site-packages
